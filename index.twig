{% extends "./templates/base.twig" %}

{% block content %}
    
        {% if meta.title == 'Home' %}

            {% include './templates/home.twig' %}

        {% else %}

            <div class="inner col-12">
                {{ content }}
            </div>

        {% endif %}

{% endblock %}